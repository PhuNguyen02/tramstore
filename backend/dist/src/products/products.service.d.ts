export declare class ProductsService {
    private readonly mockFilePath;
    private getMockData;
    findAll(): any[];
    findOne(slug: string): any;
    findByCategory(category: string): any[];
}
